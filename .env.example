# Path to the dataset (CSV or JSON). Relative paths resolved from project root.
DATASET_PATH=./data/submissions.json
DATASET_FORMAT=json

# Rules
BLOCK_ACROSS_ANY_OFFER=false
RECENT_THROTTLE_MINUTES=0

# Logging
LOG_PATH=./logs/app.log

# Validation
MIN_PHONE_DIGITS=6
